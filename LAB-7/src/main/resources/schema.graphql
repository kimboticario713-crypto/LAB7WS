type Query {
    hello: String
    getUsers: [User]
    getProducts: [Product]
}

type User {
    id: ID!
    name: String!
    email: String!
}

type Product {
    id: ID!
    name: String!
    price: Float!
    category: String!
}